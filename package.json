{
  "name": "@umbraculum/brochure",
  "private": true,
  "version": "0.0.1",
  "description": "Static marketing brochure for umbraculum.dev (Cloudflare Workers static assets).",
  "type": "module",
  "exports": {
    "./announcement": "./src/announcement.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npx --yes serve dist -p 4321",
    "deploy": "npx wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  }
}
